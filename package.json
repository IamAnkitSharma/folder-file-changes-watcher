{
  "name": "folder-file-change-watcher",
  "version": "1.0.0",
  "description": "file-change-watcher for syncing files on cloud",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev --files --respawn src/index.ts",
    "build": "npm run clean && tsc ",
    "clean": "rimraf dist/",
    "start": "node dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chokidar": "^3.5.3",
    "redis": "^4.2.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.6",
    "rimraf": "^3.0.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.4"
  }
}
